# Mission 16 필수 의존성 (PyTorch + ONNX + 시각화)
torch>=2.4.0
torchvision>=0.19.0

# 모델 양자화 및 ONNX 변환/추론
onnx>=1.15.0
onnxruntime>=1.17.0
onnxruntime-gpu>=1.17.0  # GPU 가속 (CUDA 지원 시)
onnxscript>=0.2.7

# 데이터 처리 및 시각화 (노트북 필수)
numpy<2.0.0
pandas>=2.0.0
matplotlib>=3.8.0
pillow>=10.0.0

# 머신러닝 및 분석
scikit-learn>=1.3.0

# Jupyter 노트북 환경
jupyter>=1.0.0
ipykernel>=6.29.0
jupyterlab>=4.0.0

# TensorRT 로컬 파일 기반 설치 (Windows 전용 - requirements.txt로는 불가)
# pip install D:\TensorRT\TensorRT-10.12.0.36\python\tensorrt-10.12.0.36-cp310-none-win_amd64.whl

# TensorRT 설치 이후 PyCUDA 설치(CUDA 드라이버 필요) *설치 순서 중요
# pip install pycuda==2024.1